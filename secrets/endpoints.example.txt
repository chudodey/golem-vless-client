# Golem VLESS endpoints — EXAMPLE (copy to endpoints.txt)
#
# Copy:
#   copy endpoints.example.txt endpoints.txt
# Then paste your real key(s) into endpoints.txt (that file is gitignored).
#
# Supported lines (provider-agnostic — Durev, Marzban, 3x-ui, any VLESS):
#   1) Raw VLESS URI:
#        vless://uuid@host:port?security=reality&...#Node-Name
#   2) Subscription URL (HTTPS), one per line:
#        https://example.com/sub/TOKEN
#   3) Subscription body pasted directly (base64 blob) — use this if your
#      network blocks fetching the URL above by SNI/DPI but a browser on the
#      same machine can still open it. Open the URL in a browser, copy the
#      whole response body, paste it here on its own line instead of the URL.
#   4) Optional provider label (metadata only):
#        @provider durev
#
# Comments start with #. Empty lines ignored.
# First usable outbound becomes the active proxy (override with ACTIVE= in endpoints.txt).

@provider durev
# PASTE_VLESS_OR_SUBSCRIPTION_BELOW

ACTIVE=1
